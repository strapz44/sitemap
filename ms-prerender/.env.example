# Backend environment example
# Copy this file to `.env` in the same folder and fill the values.
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster-host>/<db>?retryWrites=true&w=majority&appName=Cluster0
MONGODB_DB=prerender
PORT=3000
